# Install inside the SEED Ubuntu VM (nfstream needs libpcap-dev).
#   sudo apt-get install -y libpcap-dev
#   python3 -m pip install -r requirements.txt
nfstream>=6.5
scikit-learn>=1.2
pandas>=1.5
numpy>=1.23
matplotlib>=3.6
joblib>=1.2
